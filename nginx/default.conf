server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html; # หรือโฟลเดอร์ที่เก็บไฟล์บิลด์ของคุณ
    index index.html index.htm;

    location / {
        try_files $uri $uri/ /index.html; 
    }
}